# 🐾 Pet Marketplace

Вітаємо в Pet Marketplace — платформі для купівлі-продажу та усиновлення домашніх тварин!

## 📋 Про проект

**Pet Marketplace** — це комплексна веб-платформа, що об'єднує потенційних власників, заводчиків та притулків в єдину екосистему. На платформі ви можете:

- 🐶 Переглядати каталог чистопородних собак і котів від проверених заводчиків
- 🏠 Брати участь у процесі усиновлення тварин з притулків
- 📚 Читати статті про породи та догляд за тваринами
- 🛒 Безпечно оформляти замовлення через Stripe
- 📊 Керувати замовленнями та заявами на адопцію (для заводчиків/притулків)
- ⚙️ Управляти платформою (для адміністраторів)

## 🏗️ Архітектура проекту

Проект розділений на **дві основні частини**:

```
pet_marketplace/
├── client/              # React клієнт (фронтенд)
└── pet_marketplace_api/ # Rails API (бекенд)
```

---

## 🎨 Клієнтська частина (React)

### Розташування: `/client`

**Технологія:** React 19.2.0 + Modern JavaScript

### Основні компоненти:

- **Каталог тварин** — переглядання всіх доступних тварин
- **Пошук і фільтри** — фільтрація за породою, цінією, віком
- **Профіль користувача** — управління замовленнями та заявами
- **Панель селекціонера/притулку** — публікація та управління тваринами
- **Панель адміністратора** — управління користувачами та контентом
- **Безпеченість** — JWT аутентифікація та авторизація

### Встановлення та запуск:

```bash
# Перейти у клієнтську папку
cd client

# Встановити залежності
npm install

# Запустити розробку
npm start

# Збудувати для продакшену
npm run build
```

### Основні сторінки:

- `/` — Головна сторінка з каталогом
- `/auth` — Реєстрація та вхід
- `/pet/:id` — Деталі тварини
- `/profile` — Особистий кабінет
- `/breeder-panel` — Панель заводчика
- `/shelter-panel` — Панель притулку
- `/admin` — Адміністративна панель

---

## 🖥️ Бекенд частина (Ruby on Rails)

### Розташування: `/pet_marketplace_api`

**Технологія:** Ruby on Rails 7.1.6 + PostgreSQL

### Основні функції:

- **REST API** — всі операції доступні через API endpoints
- **Управління користувачами** — реєстрація, вхід, ролі
- **Каталог тварин** — CRUD операції для тварин
- **Замовлення та платежи** — інтеграція зі Stripe
- **Адопція** — управління заявами на адопцію
- **Контент** — статті про породи та догляд
- **Аналітика** — збір та аналіз даних
- **Фонові завдання** — Sidekiq для обробки платежів, надсилання листів

### Встановлення та запуск:

```bash
# Перейти у папку бекенду
cd pet_marketplace_api

# Встановити gems
bundle install

# Налаштувати базу даних
rails db:create db:migrate

# Запустити сервер (за умовчанням на :3000)
rails server

# Запустити Sidekiq для фонових завдань (у окремому терміналі)
bundle exec sidekiq
```

### Основні API endpoints:

```
GET    /api/pets              # Список тварин
GET    /api/pets/:id          # Деталі тварини
POST   /api/users/sign_up     # Реєстрація
POST   /api/users/sign_in     # Вхід
POST   /api/orders            # Створення замовлення
POST   /api/adoption_applications # Заявка на адопцію
POST   /api/articles          # Створення статті (адмін)
```

### Налаштування змінних оточення:

Створіть файл `.env` у папці `pet_marketplace_api`:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/pet_marketplace
RAILS_ENV=development
SECRET_KEY_BASE=your-secret-key
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
JWT_SECRET=your-jwt-secret
```

---

## 🐳 Docker (для продакшену)

### Запуск через Docker Compose:

Створіть файл `docker-compose.yml` у корені проекту:

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: pet_marketplace
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  api:
    build:
      context: ./pet_marketplace_api
    environment:
      DATABASE_URL: postgresql://postgres:password@postgres:5432/pet_marketplace
      RAILS_ENV: production
      STRIPE_SECRET_KEY: sk_test_...
    ports:
      - "3000:3000"
    depends_on:
      - postgres

  client:
    build:
      context: ./client
    ports:
      - "80:80"
    depends_on:
      - api

volumes:
  postgres_data:
```

Запуск:

```bash
docker-compose up --build
```

---

## 📦 Вимоги для розробки

- **Ruby** 3.0+
- **Node.js** 16+
- **PostgreSQL** 12+
- **Redis** (для Sidekiq)

## 🧪 Тестування

### Бекенд (RSpec):
```bash
cd pet_marketplace_api
bundle exec rspec
```

### Клієнт (Jest):
```bash
cd client
npm test
```

---

## 📚 Основні ролі користувачів

| Роль | Можливості |
|------|-----------|
| **Звичайний користувач** | Переглядання, пошук, замовлення, заявки на адопцію |
| **Заводчик** | + Додавання тварин, управління замовленнями, статистика |
| **Притулок** | + Публікація для адопції, управління заявами |
| **Адміністратор** | + Управління користувачами, модерація, аналітика |

---

## 🔐 Безпека

- ✅ JWT аутентифікація
- ✅ CORS налаштування
- ✅ Захист від CSRF атак
- ✅ Валідація всіх вхідних даних
- ✅ Зашифровані паролі (bcrypt)
- ✅ Безпечна обробка платежів Stripe

---

## 📞 Контакти та підтримка

Для питань щодо проекту звертайтесь до команди розробників.
  - client: використовувати `client/Dockerfile` для статичного сервера

Видалення/іґнорування тестів при коміті
- Якщо потрібно зробити коміт тільки з production-файлів, не включаючи тести, використайте послідовність:
  ```bash
  # створити нову гілку для production-змін
  git checkout -b production-prepare

  # додати всі файли
  git add -A

  # прибрати з індексу тестові файли (не включати у коміт)
  git reset -q HEAD -- client/src/**/*.test.* spec/ test/ || true

  # перевірити статус і створити коміт
  git status --porcelain
  git commit -m "chore: prepare production files (exclude tests)"
  git push origin production-prepare
  ```

Примітки по безпеці
- Не зберігайте секрети у репозиторії. Використовуйте змінні оточення, секретні менеджери або GitHub Secrets для CI/CD.

Пара порад для GitHub
- Додайте GitHub Actions workflow (CI) тільки для production build & deploy (без запуску тестів, якщо це дійсно вимога). Приклад job: `build-client` (npm ci && npm run build) та `build-backend` (bundle install && assets:precompile).

Файли, які я додав/оновив
- `.gitignore` — для ігнорування тестів, node_modules, логів, tmp та секретів.
- `client/Dockerfile` — multi-stage Dockerfile для збирання React та подачі через nginx.

Якщо хочете — я можу:
- додати `docker-compose.yml` приклад;
- створити простий GitHub Actions workflow для production build (без тестів);
- або підготувати clean commit (видалити тести з індексу та закомітити) — скажіть, чи ви хочете, щоб я автоматично видалив тест-файли з репо, або лише ігнорувати їх у коміті.

***
Автор: Репозиторій підготовлено для production — інструкції і Docker-ready клієнт.
***
